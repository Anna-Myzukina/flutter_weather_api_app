import 'package:flutter/material.dart';

class WeatherOnMapScreen extends StatelessWidget {
  const WeatherOnMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Map with weather will be here soon'),
      ),
    );
  }
}